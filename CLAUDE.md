# CLAUDE.md

이 파일은 Claude Code가 `his_judge_goryeo` 레포에서 작업할 때 먼저 읽는 안내 파일이다.

## 이 레포가 뭔가

history26 포털의 3차시(원 간섭기 마무리 — 권문세족의 성장) 수업용 개별 웹앱. 단일
`index.html` 하나로 구성된 정적 페이지(빌드 없음, 그대로 GitHub Pages 등에 호스팅).
핵심 질문 "권문세족의 권력 획득, 정당한 성공이었을까, 부당한 특권이었을까?"를 놓고 검사팀/
변호인팀/배심원 3역할로 나뉘어 사료를 근거로 발언문·판결문을 쓰는 역할극 활동이다.

history26의 다른 개별 웹앱들(예: `crusades`)과 같은 "역사책 페이지" 디자인 기준선을 따른다 —
세리프 본문 + 얇은 괘선 + 작은 radius + 옅은 그림자, 역할별 악센트 색만 새로 고름(검사=인장
벽돌색, 변호인=청자색, 배심원=고금 갈금색). 자세한 색 선정 이유는 `index.html` 상단 CSS
주석 참고.

## 화면 흐름

시작(학번·이름) → 도입(맥락 요약 + 핵심질문 + "실제 유죄판결 아님" 고지) → 브릿지(0단계,
원 간섭기 문제 중 하나 선택+이유 — 4차시에서 재활용 예정) → **1차 투표**(정당한 성공/부당한
특권, 아직 자료 보기 전) → 역할 확인/선택(`?role=` URL 파라미터로 자동 배정, 없으면 수동
선택) →

- **검사/변호인**: 입장 확인(고정 노출, 선택 아님) → 자료를 보며 근거 2개 선택(읽기+선택
  한 화면) → 원문을 계속 보면서 근거 요약+이유 서술 → 약점 예상하기(고른 자료 다시 보기
  패널 참고) → 4칸 발언 대본(도입/근거1/근거2/반박대응, 앞 단계 내용 자동 프리필) →
  완성된 발언문 화면(법정에서 그대로 읽는 "종이", 백엔드에 초안 백업 — 아래 참고)
- **배심원**: 원자료 전부 + 체크리스트 + 읽기 메모 2칸(검사/변호인 각각 유리한 점,
  한쪽으로 안 기울게 둘 다 필수) → 발언 듣기 메모(검사/변호인 탭 전환, 활성 팀만 입력 가능)
  → 판결문(위 메모 전체를 접이식 패널로 참고, 유죄/무죄 → 정당한 성공/부당한 특권으로
  용어 통일)

→ (공용) **최종 입장 정리** — 1차 투표와 비교해서 지금 생각을 다시 쓰는 화면 → 제출 → 완료.

**판사(반장) 역할은 v1에 있었으나 효니 요청으로 제거함**(2026-09-03). 배심원 집계·학급
대표 판결문 기능도 같이 빠짐. 디벗 전자투표도 검토했으나 배심원 서술형 판결(4단계 구조)로
대체 확정. **내장 타이머도 전부 뺐음** — 대신 "발언문 완성" 화면이 법정에서 그대로 읽는
용도로 그 역할을 대신함.

## 백엔드 연동

- `CONFIG.SHEET_WEBAPP_URL` — history26_backend 배포 URL(포털과 동일 백엔드, 하드코딩).
- `CONFIG.GAME_NAME` — 포털 커리큘럼 관리 탭에서 이 활동을 등록할 때 activity id와
  **반드시 일치**시켜야 함. 현재값 `"3차시_권문세족_역사법정"`.
- 새 backend action은 안 씀 — 기존 기본 제출 경로(`gameName` 필수, 학생당 1행)만 그대로
  호출한다.
- 최종 제출 `choicesJson`에 `role: "prosecution"/"defense"/"jury"`를 항상 포함시킨다 —
  history26_backend의 `courtCase` 조회 action과 역할극 칭호(논고왕/변론왕) 판정이 이
  필드를 읽으므로, 이 필드명·값을 바꾸면 그쪽도 같이 깨진다.
- "발언문 완성" 화면에서 `gameName + "_발언문초안"`으로 별도 백업 제출을 한 번 더 보낸다
  (`saveSpeechDraft_`). 최종 제출(최종 입장까지 마쳐야 나감)과 별개의 안전장치 — 학생이
  그 사이 이탈해도 발언문 자체는 남는다. 커리큘럼 activity id와 gameName이 안 맞아서
  진행률·포인트 계산에는 전혀 안 잡힌다(의도된 동작).

## 관련 레포

- `hyonnie-t/history26_backend` — 이 웹앱이 쓰는 백엔드(`code.gs`). `courtCase` action과
  역할극 칭호 둘 다 **2026-09-03 세션에서 추가됐지만 아직 배포 전** — 효니가 Apps Script에
  반영·재배포할 예정. 그 전까진 courtCase 호출이나 배지 지급이 실제로 동작하지 않는다.
- `hyonnie-t/re_judge` — 학생 갈등조정 도구(완전 별도 시스템, 별도 GAS_URL·스프레드시트).
  history26_backend의 `courtCase`가 이 도구의 `createCase`/`updateStatements` 페이로드
  모양대로 데이터를 만들어주지만, 코드 연동은 안 돼있음 — 교사가 결과를 복사해서 re_judge
  "새 사건 접수" 화면에 직접 넣는 반자동 방식으로 결정됨(2026-09-03). 완전 자동 연동은
  re_judge 접속 토큰을 history26_backend에 넣어야 해서 보류.

## 증거 자료 — 수정 시 반드시 지킬 것

`index.html`의 `EV` 객체가 교과서 96쪽 원문 + 국사편찬위원회 우리역사넷 교차 확인을 거친
사실 검증 완료분이다.

- 조인규·인후·기철은 실존 인물 — 새 1인칭 대사 창작 금지. `EV.jo`/`EV.in` 인터뷰는 교과서
  원문 그대로만 쓰고 확장하지 않는다.
- `EV.giGyoseo`(기철 처형 교서)는 반드시 "공민왕 자신의 정당화 문서"라는 출처 표시(`.note`
  필드)와 함께 노출할 것 — 사료 비판 훈련이 이 활동의 핵심 장치 중 하나임.
- 도입 화면의 "이건 실제 유죄 판결이 아니라 수업 장치" 고지 문구를 절대 빼지 말 것.
